1，存放店铺url的文件、存放代理IP的文件、存放账号密码的文件这三个文件必须是txt格式，使用时放在爬虫工具同一目录，txt文件名依次保存为“shop_urls”、“proxies”、“email_pwd”。
2，账号和密码必须写在指定的位置。邮箱在第一行，密码在第二行，不得有多余内容。
3，店铺的url中必须要包含	/all-wholesale-products/
3，所有的txt文件打开之后鼠标光标只能在最后一行末尾之前的位置，不能有空白行。
4，每次生成的表格文件会自动生成在爬虫工具同一目录。